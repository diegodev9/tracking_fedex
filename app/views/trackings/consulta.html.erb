<div class="consulta">
  <div class="container d-flex justify-content-center">
    <%= form_tag trackings_path, method: :get do %>
      <div class="form-group row">
        <div class="col-form-label-sm">
          <p>Ingrese código de guía</p>
          <%= text_field_tag :buscar_guia, params[:buscar_guia], class:'form-control', placeholder: 'n° de guia'%>
        </div>
        <div class="text-center mt-5">
          <%= submit_tag 'Buscar', class: 'btn btn-primary btn-md' %>
        </div>
        <!--<div class="col-2">
        <%#= link_to 'Todos', listar_pacientes_path, class: 'btn btn-primary' %>
      </div>-->
      </div>
    <% end %>
  </div>
</div>